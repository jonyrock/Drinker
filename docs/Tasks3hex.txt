*** TOODOO ***
-- Написать тесты на методы нищего
-- Использовать maven для сборки

-- isStopable - переводится как могущий остановиться, а не могущий остановить. 
   Из-за этого неверного использования этого слова возможна путаница.
Согласен, теперь это isReasonToStop(). 

*** DONE ***
-- По тестам не смог разобрать, какие методы в каждом тестируются. Выглядят громоздко и запутанно.
   Очевидно, что число объектов можно соптимизировать.
Я решил оставить старые тесты под именем drinker.TestsOld и попробовать написать новые в 
drinker.TestsNew

-- drinker.TestsOld#initWorld - проще сделать stub на OutputStream через mockito.
-- Нужно уметь просто запускаться как с прямоугольным, так и с гексагональным полями.
-- isPossibleForStep проверка на границу
-- После изменения размера поля полицейский может оказаться на рамке справа.
-- Подкрутить алгоритм кратчайшего пути на гексполе
-- Гексполе - это просто новые способы ходить по диагонали. Добавить конфиг и теперь возвращение списка
   возможных направлений.
-- Добавить поддержку гексагонального поля размером 15x15 (клетки - правильные шестиугольники).
-- Привязать пьяницу к списку направлений
-- Добавить нищего в виде z обозначения
-- Добавить в игру нищего (описание см. ниже).
-- Алгоритм поиска еще должен подсчитываеть длину и возвращать ее с парой с расстоянием
-- Добавить алгоритм поиска бутылки нищим
-- Расширить поле на одну линию снизу и добавить пункт прима стеклотары
-- Переписать абстрактное описание в конкретные задания
-- Класс WhiteSpace выпадает из словаря игры.
Теперь это называется FieldBorder. Добавлен поясняющий комментарий.



-- drinker.utils.Pair#isEqual - почему не использовать стандартный equals()?
Согласн. я просто удалил метод.


*** IGNORED ***
-- По правилам за один шаг игры каждый из героев делает по своему шагу, 
   а новый пьяница выходит из бара раз в 20 шагов игры.
А еще небо бывает голубое.
В методе World.tick() можно найти if (tickCount % 20 == 0) { addToper(); return; }

-- drinker.World#isTakePlace - логика 
            if (w.isStopAble() || w.isSuspendAble())
                return true;
    похожа на процедуру определение пола по вторичным половым признакам. Выглядит неочевидно. 
    Если в игре появится костер, он будет занимать место, но не будет ни Stopable, ни Suspendable.

Продолжая вашу аллегорию, в нашем случае пол не важен. Важны только свойства человека.  
Если появиться костер, то поведение программы не измениться. Ведь костер не претендует вести себя 
также как и бутылка. Но он может захотеть. Я сделал дизайн так, что каждый объект может переопределить
какое-то свойство из набора возможных в WorldObject. Мне никогда не понадобяться типы самих объектов -
мне выжны только их свойства на абстрактном уровне.

-- Как перевести на русский isLatencySuspendAble?
Там есть комментарий. Лучшего названия придумать не получается.
