*** TASKS ***
-- Реальная остановка после сна (проверить положение)
-- Проверка что никого не было на месте фонаря
-- Проверка что полицейский хоть раз встречался с пьяницей

*** DONE ***
-- в клетку с координатами 16, 0 нельзя походить
-- в клетку с координатами 0, 7 можно походить
-- при попытке шагнуть в клетку со столбом пьяница засыпает
-- при шаге в клетку с бутылкой пьяница падает и засыпает
-- при столкновении с другим пьяницей пьяница остается бодрствующим и стоит на ногах
-- Списать юнит-тесты с правил игры (ниже реальные тесты)
---- Мои задачи ----
-- Навести порядок с папками
-- Отдельный класс собитий WorldEvent
-- Тест что только 1 персонаж передвинулся
-- Проверить через 20 шагов появление пьяницы
-- Сон пьяницы если столкнулся с другим спящиим пьяницей

*** Замечания ***
1. Я собираюсь тестировать состояние, а не поведение пьяницы, 
   потому что это проще. Т.е. я проверяю флаг заснул ли он вместо
   того что бы провертиь двигался ли он на следующем шаге.
2. Я очень много использую проверку на типы в тестах. Вообще говоря,
   это плохая практика, но это тесты...
3. Задание: при столкновении с другим пьяницей пьяница остается бодрствующим и стоит на ногах.
   Но в клетке где был пьяница могла еще быть бутылка. Поэтому тест был усложнен.
4. Когда происходит проверка Assert.assertFalse(message, boolean) то в message 
   должно быть то что произошло плохое или то что должно было произойти по-хорошему? 
5. Часто один тест тестирует несколько аспектов (много asserts)
6. Тесты могут показаться странными, т.к. буквально проверяют точно тоже самое, что написано в коде,
   но они хорошо проверяют что правильно работают события